from module_22 import add;
from module_23 import multiply;

# module and packages 
# module = a file 
# package = a folder 
# - in python all the definitions (functions,classes,variables,etc)are publ

print(add(1,2))
print(multiply(2,3))